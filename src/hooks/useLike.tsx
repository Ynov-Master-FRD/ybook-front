import axios from "axios";


export const useLikeButton = (postId: number) => {
    
    // useAuth

    // apiBack.post(`/post/like/${id}`, {
    //     userId: 18
    //   })
    //   .then((response) => {
    //       console.log(response);

    //   }, (error) => {
    //       console.log(error);
    //   });
}